import 'package:flutter/material.dart';

class SettingTaps extends StatelessWidget {
  const SettingTaps({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
